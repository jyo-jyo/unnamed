{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "publish": "lerna run tsc && lerna publish"
  },
  "devDependencies": {
    "lerna": "^5.1.6"
  },
  "name": "unnamed"
}
